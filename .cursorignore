# Cursor ignore file
# Files and directories listed here will be ignored by cursor AI features
# Format: each line is a glob pattern

# Only exclude truly sensitive files, allow everything else
*.key
*.pem
*.p12
*.pfx

# Explicitly allow important project files and folders
!node_modules
!vendor
!.env
!*.cer
!*.crt
!*.der
!*.priv
!*.keystore
!*.jks
!*.p8 